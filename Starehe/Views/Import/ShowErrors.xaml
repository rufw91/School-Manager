<controls:CustomWindow x:Class="UmanyiSMS.Views.ShowErrors"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:Helper.Controls;assembly=Helper"
        Title="Errors" Height="450" Width="400" ResizeMode="NoResize" WindowStartupLocation="CenterScreen">
    <Grid>
        <TextBlock HorizontalAlignment="Left" Margin="10,10,0,0" Text="Error(s)" VerticalAlignment="Top"
                   Style="{DynamicResource ContentTitle}"/>
        <ListBox Margin="10,50,10,50" ItemsSource="{Binding Processed}" Style="{x:Null}"/>
        <Button Content="Close" IsCancel="True" HorizontalAlignment="Right" Margin="0,0,10,10" VerticalAlignment="Bottom" Width="100"/>

    </Grid>
</controls:CustomWindow>
