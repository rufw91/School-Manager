<UserControl x:Class="UmanyiSMS.Views.ClassLeavingCertificates"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
        d:DesignHeight="600" d:DesignWidth="900"
	Background="White">

    <Grid>

        <TextBlock HorizontalAlignment="Left" Margin="20,20,0,0" Style="{DynamicResource Title}"  Text="Class Leaving Certificates" VerticalAlignment="Top"/>
        <TextBlock HorizontalAlignment="Left" Margin="33,80,0,0" Text="Class" VerticalAlignment="Top"/>
        <ComboBox Margin="33,100,0,0" VerticalAlignment="Top" HorizontalAlignment="Left" Width="250"
                  ItemsSource="{Binding AllCombinedClasses}" DisplayMemberPath="Description"
                  SelectedItem="{Binding SelectedCombinedClass, Mode=OneWayToSource, UpdateSourceTrigger=PropertyChanged}" 
                  />

        <TextBlock HorizontalAlignment="Right" Margin="0,38,20,0" VerticalAlignment="Top" Grid.Column="1">
            <Hyperlink Command="{Binding RefreshCommand}">Fetch Data</Hyperlink>
        </TextBlock>
        <Button Content="Generate" HorizontalAlignment="Right" Margin="0,0,10,10" VerticalAlignment="Bottom" Width="100"
                Command="{Binding GenerateCommand}"/>

    </Grid>
</UserControl>
