<UserControl x:Class="Starehe.Views.PaymentVoucherHistory"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignHeight="700" d:DesignWidth="984"
             Background="White">
    <Grid>
        <TextBlock HorizontalAlignment="Left" Margin="20,20,0,0" Style="{DynamicResource Title}"  Text="Payment Voucher History" 
                   VerticalAlignment="Top"/>
        <DataGrid Margin="20,80,20,20" AutoGenerateColumns="False" IsReadOnly="True" CanUserDeleteRows="False" CanUserReorderColumns="False"
                  CanUserResizeColumns="True" ItemsSource="{Binding AllPaymentVouchers}">
            <DataGrid.Columns>
            <DataGridTextColumn Header="Name of Payee" Binding="{Binding NameOfPayee}" Width="*" IsReadOnly="True"/>
                <DataGridTextColumn Header="Address" Binding="{Binding Address}" Width="*" IsReadOnly="True"/>
                <DataGridTextColumn Header="Total" Binding="{Binding Total}" Width="*" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>
                  
    </Grid>
</UserControl>
