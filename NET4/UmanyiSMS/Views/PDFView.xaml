<UserControl x:Class="UmanyiSMS.Views.PDFView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Helper.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="984"
             Background="White">
    <Grid>
        <TextBlock HorizontalAlignment="Left" Margin="20,20,0,0" Style="{DynamicResource Title}"  Text="PDF View" 
                   VerticalAlignment="Top"/>

        <local:PdfViewerHost x:Name="PdfViewer" PdfPath="{Binding CurrentPdf}" Margin="10,60,10,10"/>
    </Grid>
</UserControl>
